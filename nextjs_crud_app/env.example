#Create a .env file in the root of your project and add the above variables before running the application.


# MongoDB connection string (e.g., from MongoDB Atlas or local MongoDB instance)
MONGO_URI=mongodb://localhost:27017/your-database-name

# Base URL of the frontend app (used for client-side API calls)
NEXT_PUBLIC_API_URL=http://localhost:3000